require 'spec_helper'

describe SessionObserver do
  # subject { SessionObserver.instance }

  # it 'calls #after_commit when session is saved' do
  #   session = FactoryGirl.create(:session)
  #   session.is_authenticated = ! session.is_authenticated
  #   subject.should_receive(:after_commit).with(session)
  #   session.save
  # end
end